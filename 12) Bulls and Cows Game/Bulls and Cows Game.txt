Чтобы применить принципы ООП, разделим логику игры на несколько классов.

1. Класс Game (Основная логика)
Этот класс будет содержать основную логику игры:

Секретное число: Генерируется при старте игры.

Метод start(): Запускает цикл игры.

Метод checkGuess(String guess): Сравнивает догадку игрока с секретным числом и возвращает результат (количество "быков" и "коров").

2. Класс BullsAndCowsApp (Ввод/вывод)
Этот класс будет точкой входа (main) и будет отвечать за взаимодействие с консолью: считывание ввода игрока и вывод результатов

🧠 Задачи для реализации
1. Реализация generateSecretNumber() (Логика)
Начальный уровень требует реализации метода, который генерирует уникальное 4-значное число (например, не 1123, не 0123).

Совет:

Создайте список цифр от 0 до 9.

Перемешайте его (Collections.shuffle()).

Возьмите первые 4 цифры (убедитесь, что первая не 0, если это важно).

2. Валидация ввода (Логика/Обработка ошибок)
Ваша программа должна проверять, что ввод игрока:

Состоит ровно из 4 символов.

Состоит только из цифр.

Не содержит повторяющихся цифр.

Используйте циклы, условия (if), и, возможно, try-catch для обработки ошибок.

3. Отображение истории ходов (ООП/Логика)
Для улучшения функционала вы можете создать отдельный класс GuessHistory или просто использовать список (List<String>) в классе BullsAndCowsApp, чтобы отображать игроку его предыдущие ходы и их результаты.

Это даст вам прочную основу, демонстрирующую умение работать с ООП (разделение на классы Game и BullsAndCowsApp) и логикой (методы генерации и проверки).

Желаете, чтобы я подробнее объяснил, как реализовать метод generateSecretNumber()?
